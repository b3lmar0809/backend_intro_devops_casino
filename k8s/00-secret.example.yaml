# PLANTILLA — valores de ejemplo. NO contiene secretos reales.
# Copiar a 00-secret.yaml, poner valores reales y aplicar SOLO localmente (no commitear).
apiVersion: v1
kind: Secret
metadata:
  name: casino-secrets
type: Opaque
stringData:
  DB_USER: "casino"
  DB_PASSWORD: "CAMBIAR_EN_LOCAL"
  DB_NAME: "casino_db"
  JWT_SECRET: "CAMBIAR_EN_LOCAL"
